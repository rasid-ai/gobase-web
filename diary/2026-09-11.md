# 2026-09-11 — Moving the repo to an org, and the GHCR credential trap

## 11:40 — The owner change: what breaks and what does not

No code changed. This session was diagnosis and a deploy plan.

The repo moved from a personal GitHub account to an organization. The
question was what has to change so the VPS can still pull its images from
GHCR (GitHub's container registry, where the built images live).

### What was found

**Nothing in the repo needs editing.** The owner is never hardcoded. CI
builds the image name from `github.repository_owner` at run time and
lowercases it (`.github/workflows/deploy.yml:59`); the VPS builds it from
`GHCR_OWNER` in its own `.env` (`infra/docker-compose.prod.yml:14`). A grep
for the old owner name across the repo returns nothing. So the whole change
is two host-side values and some org settings.

**GHCR packages do not move with a repo transfer.** Packages belong to the
account, not the repo. The org therefore has no images at all until CI runs
again, and the pre-transfer images stay under the personal account. Order
matters: push first, then deploy, or `deploy.sh` pulls a tag that does not
exist yet.

**Rollback across the transfer is broken.** `deploy.sh` pulls
`ghcr.io/$GHCR_OWNER/...`, so rolling back to any SHA built before the move
fails — those images only exist under the old owner. Recovering one means
setting `GHCR_OWNER` back by hand, or re-pushing the old tags to the org.
This defeats the "a rollback is a single command" promise in
`infra/deploy.sh:6`.

### Decisions

- **Isolate this project's GHCR login with `DOCKER_CONFIG`, rather than
  logging in again as the `deploy` user.** Docker stores one credential per
  registry host, so `docker login ghcr.io` overwrites whatever is in
  `~/.docker/config.json`. The deploy VPS runs other projects that pull from
  the same host, and they would have broken at their next deploy, not
  immediately. Pointing `DOCKER_CONFIG` at `/srv/geo-portal/.docker` gives
  this project its own credential file and leaves the shared one untouched.

  Rejected: a second `docker login` as the `deploy` user (silently breaks
  the neighbours), and a separate Linux user per project (larger change to
  a host that already works).

  It needs no repo change. `infra/deploy.sh:17-19` wraps the `.env` load in
  `set -a` / `set +a`, so every value in `.env` is already exported to the
  `docker compose` calls that follow. One line in the host `.env` is enough.

- **Use one classic PAT on the personal account, scoped `read:packages`.**
  That scope follows what the token's owner can read, so a single token
  covers both the org's new packages and the personal account's old ones —
  which is what a rollback to a pre-transfer SHA needs. An org-scoped or
  machine-account token could not read both.

### Open threads

- **None of it is done yet.** The org settings, the PAT, the `.env` edits
  and the first push are all still to do. Nothing has been run against
  either VPS.
- **Two hosts, not one.** `infra/README.md:7` says the firm's VPS deploys
  from `main` and the developer VPS from `dev`. Each has its own `.env` and
  its own Docker credential file, so both need the same treatment. Deploy
  `dev` first — same pipeline, lower stakes.
- **`DOCKER_CONFIG` only applies through `./deploy.sh`.** Compose reads
  `.env` for substitution into the compose file, not for the Docker CLI's
  own config directory. A hand-typed `docker compose pull` on that host will
  miss it and fail.
- **Org settings are the likely first failure.** Third-party actions must be
  allowed (the workflow uses five), package creation must be permitted, and
  a classic PAT needs SSO authorization if the org enforces SAML. An
  unauthorized PAT returns `denied`, which reads like a wrong scope.
- **`KB_DB_PORT` is wrong in the local `.env`.** It says 5433, but nothing
  listens there and `pg_lsclusters` shows a single cluster on 5432 — which
  is what docs/adr/002 describes. The `kb` role does exist on 5432. Unused
  so far, so it has never failed; the Maps work is the first thing that
  will hit it.
- **`infra/README.md` section 2b does not mention the credential
  collision.** It covers ports, nginx and image pruning for a shared host,
  and stops there. The collision is the one that would actually have caused
  an outage.

### Next session starts with

- Deciding whether to write up the `DOCKER_CONFIG` isolation and the
  rollback caveat in `infra/README.md` 2b and `infra/.env.example` — offered
  this session, not yet answered. Then the org move itself, `dev` VPS first.

Local dev-stack problems from earlier today (container DNS, a stale
frontend image, Postgres peer auth) were machine-level and are in the global
diary for 2026-09-11, not here.

## 19:30 — GP-4 and GP-5: vector data from the lake, drawn on the map

Jira: GP-1 (epic Map) · GP-4, GP-5 built · GP-6..GP-11 open

### Correction to the 11:40 entry

That entry lists `KB_DB_PORT=5433` as wrong in the local `.env`. It was
right to flag it but wrong about the fix, and the reasoning was circular:
`context/integrations/kb.md` claimed a separate `gobase-test-pg` container
on 5433, which was itself stale. The truth is docs/adr/002 as written —
`geo_kb` and `portal` are two databases in **one** instance, so the port is
`portal`'s. kb.md has been corrected.

### What changed

**`backend/apps/map/lake.py` is new** and is the only module that imports
duckdb. `GET /api/map/assets/{id}/data` returns an asset's GeoParquet rows
as GeoJSON. Seven `LAKE_S3_*` settings, no provider named in code.

**The frontend draws it.** A layer store scoped to the workspace, a
`LayersPanel`, a `Draw` control per asset row, and a new `ui/Toast`
primitive for the failure path. Three map layers per asset — fill, line,
circle — because 10 of the 22 catalog layers are `Mixed`.

Four commits on `Maps`: b2ee84b, 2f952bd, 7464c77, 96378bc. 69 backend
tests (was 50), 44 frontend (was 37). New: docs/adr/008,
context/integrations/lake.md.

### Decisions

- **The lake path is `assets.source_uri`, not `geo_layers.parquet_uri`.**
  Verified identical for all 22 active assets, and `assets` carries every
  modality, so the join buys nothing. GP-4's description says otherwise and
  invents an `Asset` model and a `silver_path` column; neither exists.

- **`LAKE_SILVER_BUCKET` was dropped.** `source_uri` is already a complete
  `s3://` URI with the bucket in it.

- **Layers outlive the selection**, reversing specs/map.md. With 22 assets
  over one city, comparison is the reason to draw features at all. ADR-008.

- **A modality-specific endpoint may name its own modality.** Also ADR-008.
  The original rule was about never holding a *closed list* of types for
  grouping or display; that still stands everywhere else.

- **Region is config, not a constant.** RustFS/MinIO have no regions; AWS
  needs the bucket's. Blank is passed through and the field is omitted.

### Problems solved

- **Every S3 read returned HTTP 404 for a file that was really there.**
  `SET s3_*` is session-scoped, and every request runs on
  `connection.cursor()`, which opens its own session and inherits none of
  it — so the store was reached with no configuration at all. Fix:
  `CREATE SECRET`, which lives in the database and is shared by cursors.
  GP-4's own implementation note claims a cursor "shares extensions and
  config"; it shares extensions only. Regression test added — the
  local-path tests cannot catch it, since a local path needs no S3 config.

  The error text names an empty region, which sent me down a dead end. The
  region is irrelevant: `''`, `us-east-1` and `banana` all read the same
  file against a live MinIO.

- **`items: {}` in the contract typed features as `unknown[]`.** A plain
  `ListField` declares nothing. Fix: `FeatureListField` with
  `@extend_schema_field`, so Orval emits a typed array.

### Open threads

- **`_UNAVAILABLE_MARKERS` in `lake.py` is a substring heuristic.** DuckDB
  reports "unreachable" and "unreadable" both as `IOException`, and that
  list is the only thing separating 503 from 502. A DuckDB upgrade that
  rewords a message breaks the split silently. Nothing in CI pins it.
- **Raster has no local data at all.** `geo_raster_layers` is empty, so
  GP-10/GP-11 cannot be built against anything real. The reverse of what
  kb.md used to claim.
- **One asset, one layer** holds for all 22 active assets today. The
  response shape assumes it. A multi-layer asset would need a new shape.
- **`fitBounds` is untested.** The map is mocked in jsdom and the ref is
  null there, so the zoom-to-extent path never runs in a test.
- **GP-4 and GP-16 descriptions are wrong in Jira** — see decisions above,
  and GP-16 still names `/launch/` where the endpoint is `/api/runs/trigger`.

### Next session starts with

- GP-6/GP-7, draw-area mode — the next pair in the epic, and the one that
  needs the Ask path. GP-8..GP-11 depend on answers and on raster data that
  does not exist locally yet.
