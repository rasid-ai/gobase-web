# 2026-09-01 — Repo bootstrap session

**Tickets / requirement IDs touched:** HLR-001–026 (created)

**What changed:** Repo scaffolded per the AI Development Standard v0.1.
Written this session: CLAUDE.md (non-negotiables + pointers);
context/project-overview.md, architecture.md, code-standards.md,
ui-rules.md, design-system.md (interim tokens rule), ui-registry.md
(seed); ADR-001 (adopt Rainbow reference architecture), ADR-002 (two
Postgres databases, one instance), ADR-003 (MapLibre over Leaflet),
ADR-004 (Dagster via GraphQL only, single launchRun mutation);
specs/auth.md (HLR-001–008), specs/runs.md (009–013), specs/ask.md
(014–021), specs/map.md (022–026). Mid-session the app went from three
pages to two: chat lives inside the Map workspace; context files were
rewritten accordingly.

**What broke:** Nothing — no code exists yet.

**Open threads:**
- Q&A engine (LLM provider + agent design) deliberately parked — it is
  the first Gate B decision and gets its own ADR before the chat app is
  built.
- Design tokens await the delivered design; design-system.md carries the
  interim rule.
- Specs are drafts: Gate A (/spec-review + product-owner sign-off) has
  not run. No Jira tickets generated yet.
- Traceability check script not yet written or wired into CI.
- Deploy host secrets for the two VPSes not yet configured.

**Next session starts with:** Gate A — run /spec-review over the four
specs, fix wording, sign off, then generate Jira tickets from them.
After that, Gate B (/architect) for the Q&A engine ADR.
